// Placeholder — replaced by the render/UI agent with the Lab (2-map view + machine
// palette + run). Kept minimal so the Vite/React toolchain typechecks and builds.
export function App() {
  return <h1>HexaPharma Lab</h1>;
}
